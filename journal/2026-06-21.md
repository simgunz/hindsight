# 2026-06-21

## What was done
- Completed bootstrap Phases 3–4 (tech stack + walking skeleton).
- **Phase 3:** 9 ADRs (one decision each; only tooling grouped), ordered foundational→app-specific; Phase 2 requirements approved.
- **Phase 4:** Vite + vanilla-TS app scaffolded; **live in production** on `hindsight.simgunz.org` (Netlify, HTTPS/Let's Encrypt).
- Git CD wired: `main`→prod, `staging`→branch deploy, PRs→previews.
- CI gate (GitHub Actions): parallel `lint`+`test` jobs, Node pinned via `.nvmrc`.
- Installable PWA (manifest + service worker + icons) via vite-plugin-pwa.
- README + CLAUDE.md (conventions & workflow) added.
- Merged `staging`→`main`: skeleton live in production, PWA verified.
- Distilled Phase 4 learnings into `bootstrapping-apps` skill (Context7 doc-currency, CI two-job/Node-LTS, new `web-vite-spa` stack file, PWA correctness) + zettelkasten Netlify note.

## Decisions & rationale
- **Web PWA** (vs native): single-user tool, instant CD, browser covers camera+buffer.
- **Vanilla TS + Vite**: app is one imperative media loop, not a component tree → best debuggability.
- **WebCodecs buffer (ADR 0009)**: one technique for Android+iPhone (modern Safari 26 has full WebCodecs); only camera-capture (`FrameSource`) is platform-branched. Left `proposed` pending on-device spike.
- **Branch model**: `main` = stable/prod only; active work on `staging`.
- Validate tool **config** (not just versions) via Context7 — caught Biome `recommended`→`preset`.

## Open questions / blockers
- **ADR 0009 (WebCodecs)** still `proposed` — must be validated on a real phone before building features.
- Minor: Netlify serves the manifest as `application/octet-stream` (works; `application/manifest+json` is proper) — optional header fix.

## Next steps
- **WebCodecs spike** (throwaway): `getUserMedia` → `FrameSource` → `VideoEncoder` → ring buffer → `VideoDecoder` → canvas, delayed N s; deploy to a branch URL; test on phone (Android first, then iPhone); measure 30fps throughput, latency, memory. → flip ADR 0009 to `approved` or pivot to fallback.
- Then start the delayed-mirror feature implementation per approved requirements.
