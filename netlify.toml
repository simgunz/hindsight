[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Single entry point — route all paths to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
